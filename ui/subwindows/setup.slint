import { GridBox } from "std-widgets.slint";
export component FirstSetup inherits Dialog {
    default-font-size: 16px;
    GridBox {
        Row {
            Text {
                text: "Rust4Diva Setup";
            }
        }

        Row {
            Text {
                text: "Rust4Diva Setup";
            }
        }
    }
}
